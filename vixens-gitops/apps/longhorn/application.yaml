# application.yaml - Configuration officielle
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: longhorn
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://charts.longhorn.io
    chart: longhorn
    targetRevision: 1.6.0
    helm:
      values: |
        # Configuration Talos OS officielle
        global:
          cattle:
            systemDefaultRegistry: ""
        
        # Désactiver complètement les hooks problématiques
        preUpgradeChecker:
          enabled: false
        
        # Settings optimisés
        defaultSettings:
          createDefaultDiskLabeledNodes: true
          defaultDataPath: /var/lib/longhorn
          replicaSoftAntiAffinity: false
          storageOverProvisioningPercentage: 200
          storageMinimalAvailablePercentage: 25
        
        persistence:
          defaultClass: true
          defaultClassReplicaCount: 2
        
        ingress:
          enabled: false
        
        service:
          ui:
            type: ClusterIP
        
        # Configuration Talos OS
        longhornManager:
          tolerations:
            - key: "node-role.kubernetes.io/control-plane"
              operator: "Exists"
              effect: "NoSchedule"
            - key: "node-role.kubernetes.io/master"
              operator: "Exists"
              effect: "NoSchedule"
          securityContext:
            privileged: true
        
        longhornDriver:
          tolerations:
            - key: "node-role.kubernetes.io/control-plane"
              operator: "Exists"
              effect: "NoSchedule"
            - key: "node-role.kubernetes.io/master"
              operator: "Exists"
              effect: "NoSchedule"
          securityContext:
            privileged: true
        
        # Réduction des ressources
        csi:
          attacherReplicaCount: 1
          provisionerReplicaCount: 1
          resizerReplicaCount: 1
          snapshotterReplicaCount: 1
        
        webhook:
          replicas: 1
  destination:
    server: https://kubernetes.default.svc
    namespace: longhorn-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - SkipHooks=true  # Force l'ignorance des hooks
      
      
      
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-media-management-readarr-config
  labels:
    stack: media-management
    container: readarr
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-media-management-readarr-config

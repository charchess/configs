apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-media-management-mylar-config
  labels:
    stack: media-management
    container: mylar
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-media-management-mylar-config

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-tools-calibre-.cache
  labels:
    stack: tools
    container: calibre
    volume: .cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-tools-calibre-.cache

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-tools-calibre-config
  labels:
    stack: tools
    container: calibre
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-tools-calibre-config

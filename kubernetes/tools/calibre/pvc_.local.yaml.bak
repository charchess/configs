apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-tools-calibre-.local
  labels:
    stack: tools
    container: calibre
    volume: .local
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-tools-calibre-.local

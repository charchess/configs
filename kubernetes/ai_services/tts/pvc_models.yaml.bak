apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-ai_services-tts-models
  labels:
    stack: ai_services
    container: tts
    volume: models
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-ai_services-tts-models

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-ai_services-stt-models
  labels:
    stack: ai_services
    container: stt
    volume: models
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-ai_services-stt-models

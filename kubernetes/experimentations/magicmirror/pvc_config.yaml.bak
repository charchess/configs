apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-magicmirror-config
  labels:
    stack: experimentations
    container: magicmirror
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-magicmirror-config

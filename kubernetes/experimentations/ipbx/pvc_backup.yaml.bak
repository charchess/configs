apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-ipbx-backup
  labels:
    stack: experimentations
    container: ipbx
    volume: backup
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-ipbx-backup

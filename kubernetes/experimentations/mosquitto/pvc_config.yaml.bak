apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-mosquitto-config
  labels:
    stack: experimentations
    container: mosquitto
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-mosquitto-config

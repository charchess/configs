apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-mosquitto-data
  labels:
    stack: experimentations
    container: mosquitto
    volume: data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-mosquitto-data

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-calibre-web-config
  labels:
    stack: experimentations
    container: calibre-web
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-calibre-web-config

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-emby-config
  labels:
    stack: experimentations
    container: emby
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-emby-config

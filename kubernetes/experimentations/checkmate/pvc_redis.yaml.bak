apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-checkmate-redis
  labels:
    stack: experimentations
    container: checkmate
    volume: redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-checkmate-redis

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-airsonic-playlists
  labels:
    stack: experimentations
    container: airsonic
    volume: playlists
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-airsonic-playlists

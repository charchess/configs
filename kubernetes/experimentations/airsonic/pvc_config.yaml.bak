apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-airsonic-config
  labels:
    stack: experimentations
    container: airsonic
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-airsonic-config

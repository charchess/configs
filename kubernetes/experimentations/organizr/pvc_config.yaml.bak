apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-organizr-config
  labels:
    stack: experimentations
    container: organizr
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-organizr-config

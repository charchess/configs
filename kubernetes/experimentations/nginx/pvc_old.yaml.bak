apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-nginx-old
  labels:
    stack: experimentations
    container: nginx
    volume: old
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-nginx-old

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-bazarr-config
  labels:
    stack: experimentations
    container: bazarr
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-bazarr-config

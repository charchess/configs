apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-overseer-config
  labels:
    stack: experimentations
    container: overseer
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-overseer-config

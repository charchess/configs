apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-appdaemon-conf
  labels:
    stack: experimentations
    container: appdaemon
    volume: conf
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-appdaemon-conf

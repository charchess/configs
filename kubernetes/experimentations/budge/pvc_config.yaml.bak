apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-budge-config
  labels:
    stack: experimentations
    container: budge
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-budge-config

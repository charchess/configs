apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-influxdb2-lib
  labels:
    stack: experimentations
    container: influxdb2
    volume: lib
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-influxdb2-lib

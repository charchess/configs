apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-graylog-opensearch_data
  labels:
    stack: experimentations
    container: graylog
    volume: opensearch_data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-graylog-opensearch_data

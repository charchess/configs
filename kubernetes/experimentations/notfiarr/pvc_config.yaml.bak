apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-notfiarr-config
  labels:
    stack: experimentations
    container: notfiarr
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-notfiarr-config

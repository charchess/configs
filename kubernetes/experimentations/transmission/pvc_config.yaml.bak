apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-transmission-config
  labels:
    stack: experimentations
    container: transmission
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-transmission-config

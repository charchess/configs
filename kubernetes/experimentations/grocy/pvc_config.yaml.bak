apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-grocy-config
  labels:
    stack: experimentations
    container: grocy
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-grocy-config

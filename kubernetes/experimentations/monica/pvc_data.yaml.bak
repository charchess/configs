apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-monica-data
  labels:
    stack: experimentations
    container: monica
    volume: data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-monica-data

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-redmine-files
  labels:
    stack: experimentations
    container: redmine
    volume: files
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-redmine-files

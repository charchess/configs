apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-jackett-Jackett
  labels:
    stack: experimentations
    container: jackett
    volume: Jackett
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-jackett-Jackett

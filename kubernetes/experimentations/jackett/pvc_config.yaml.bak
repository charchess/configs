apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-jackett-config
  labels:
    stack: experimentations
    container: jackett
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-jackett-config

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-cops-config
  labels:
    stack: experimentations
    container: cops
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-cops-config

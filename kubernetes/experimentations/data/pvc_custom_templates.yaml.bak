apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-experimentations-data-custom_templates
  labels:
    stack: experimentations
    container: data
    volume: custom_templates
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-experimentations-data-custom_templates

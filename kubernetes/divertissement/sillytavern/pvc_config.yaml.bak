apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-divertissement-sillytavern-config
  labels:
    stack: divertissement
    container: sillytavern
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-divertissement-sillytavern-config

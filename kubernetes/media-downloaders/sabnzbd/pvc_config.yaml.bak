apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-media-downloaders-sabnzbd-config
  labels:
    stack: media-downloaders
    container: sabnzbd
    volume: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-media-downloaders-sabnzbd-config

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-databases-neo4j-data
  labels:
    stack: databases
    container: neo4j
    volume: data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  volumeName: pv-pvc-databases-neo4j-data
